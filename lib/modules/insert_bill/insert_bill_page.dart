import 'package:flutter/material.dart';

class InsertBillPage extends StatelessWidget {
  const InsertBillPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Insert Bill Page'),
      ),
    );
  }
}
